# Images for the portfolio

Drop your screenshots here with these **exact filenames** so the pages pick them up.
Until a file exists, the page shows a dashed "尚未上傳" placeholder instead of breaking.

## Project 1 — Non Conform data processing
| Filename | What it is |
|---|---|
| `non-conform-overview.png`   | The project overview slide (Excel columns + KPI + chart description) |
| `non-conform-dashboard.png`  | The Non Conform Report dashboard screenshot |
| `non-conform-excel.png`      | The raw T100 csfr019 Excel export |
| `non-conform-comparison.png` | The before/after (2hrs → 5min) slide *(optional — already rebuilt in HTML)* |

## Project 2 — OCAC education promotion (not uploaded yet)
| Filename | What it is |
|---|---|
| `ocac-1.jpg` | Slide 1 |
| `ocac-2.jpg` | Slide 2 |
| `ocac-3.jpg` | Slide 3 |

> Need more OCAC slides? Add `ocac-4.jpg`, etc., then copy one `<div class="slide">…</div>`
> block in `projects.html` and point it at the new file.

## How to add the files
- **GitHub web:** open this `images/` folder → **Add file → Upload files** → drag your
  screenshots in (rename them to match the table) → **Commit changes**.
- **Locally:** copy the files into this folder, then `git add images/ && git commit && git push`.
